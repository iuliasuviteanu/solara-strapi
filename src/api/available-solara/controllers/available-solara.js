'use strict';

/**
 * available-solara controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::available-solara.available-solara');
